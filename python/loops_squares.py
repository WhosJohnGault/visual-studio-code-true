squares=[]
for value in range(1,11):  
    square=value**2               #value**2 denotes taking a number from range and then raising it by a square.
    squares.append(square)         #when the loop is being ran the value is squared and added to the list.
print(squares)